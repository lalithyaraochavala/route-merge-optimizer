export default function Home() {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-3 px-6 text-center">
      <h1 className="font-[family-name:var(--font-display)] text-3xl font-bold text-foreground">
        Route Merge Optimizer
      </h1>
      <p className="max-w-md font-[family-name:var(--font-body)] text-foreground-muted">
        Dashboard scaffold placeholder — data layer and UI land in later
        phases.
      </p>
    </div>
  );
}
